class Solution(object):
    def threeSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        i, j, k = None, None, None
        for i in range(1, len(nums)):
            if nums[]


sol = Solution()
print(sol.threeSum())